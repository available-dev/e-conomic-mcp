{
	"$schema": "http://json-schema.org/draft-03/schema#",
	"title": "Currencies collection Schema",
	"description": "A schema for retrieving a collection of currencies.",
	"type": "object",
	"restdocs": "http://restdocs.e-conomic.com/#get-currencies",
	"properties": {
		"collection": {
			"type": "array",
			"description": "A collection of the currencies.",
			"items": {
				"title": "Currency schema",
				"type": "object",
				"description": "A schema for information about a currency.",
				"properties": {
					"name": {
						"type": "string",
						"maxLength": 50,
						"description": "The name of the currency."
					},
					"code": {
						"type": "string",
						"maxLength": 3,
						"description": "The ISO 4217 code of the currency."
					},
					"isoNumber": {
						"type": "string",
						"maxLength": 3,
						"description": "The ISO 4217 numeric code of the currency."
					},
					"self": {
						"type": "string",
						"format": "uri",
						"description": "The unique self reference of the currency resource.",
						"required": true
					}
				}
			}
		},
		"metaData": {
			"type": "object",
			"description": "Information about possible actions, endpoints and resource paths related to the endpoint."
		},
		"pagination": {
			"type": "object",
			"description": "Information about the pagination."
		},
		"self": {
			"type": "string",
			"format": "uri",
			"description": "The unique self reference of the accounts collection.",
			"required": true
		}
	}
}