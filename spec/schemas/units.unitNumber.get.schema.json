{
	"$schema": "http://json-schema.org/draft-03/schema#",
	"title": "Unit GET schema",
	"description": "A schema for retrieval of a single unit.",
	"type": "object",
	"restdocs": "http://restdocs.e-conomic.com/#get-units-unitnumber",
	"properties": {
		"unitNumber": {
			"type": "integer",
			"description": "A unique identifier of the unit."
		},
		"name": {
			"type": "string",
			"maxLength": 100,
			"description": "The name of the unit."
		},
		"products": {
			"type": "string",
			"format": "uri",
			"description": "A link to the products that reference this unit."
		},
		"self": {
			"type": "string",
			"format": "uri",
			"description": "A unique link reference to the unit item.",
			"required": true
		}
	}
}