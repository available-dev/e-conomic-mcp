{
	"$schema": "http://json-schema.org/draft-03/schema#",
	"title": "App roles schema",
	"description": "A schema for retrieving an app role.",
	"type": "object",
	"restdocs": "http://restdocs.e-conomic.com/#get-app-roles-rolenumber",
	"properties": {
		"roleNumber": {
			"type": "integer",
			"minimum": 1,
			"filterable": true,
			"sortable": true,
			"defaultSorting": "ascending",
			"description": "A reference number for the app role."
		},
		"name": {
			"type": "string",
			"enum": ["SuperUser", "Bookkeeping", "Sales", "ProjectEmployee"],
			"description": "API Role name."
		},
		"requiredModules": {
			"title": "Required modules",
			"type": "array",
			"description": "An array containing the required modules.",
			"items": {
				"type": "object",
				"description": "An array of the required modules needed.",
				"properties": {
					"moduleNumber": {
						"type": "integer",
						"minimum": 1,
						"description": "A reference number for the module."
					},
					"self": {
						"type": "string",
						"format": "uri",
						"description": "A unique reference to the module resource.",
						"required": true
					}
				}
			}
		}
	}
}